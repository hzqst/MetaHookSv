# ~/.codex/config.toml
project_doc_fallback_filenames = [".claude/CLAUDE.md"]

# Always ask for approval mode
approval_policy = "never"
sandbox_mode    = "workspace-write"

[mcp.servers.basic-memory]
command = "basic-memory"
args = ["mcp", "--project", "metahooksv"]